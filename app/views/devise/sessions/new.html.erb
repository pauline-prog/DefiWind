



<div class="bg-log">
  <%= render 'components/navbar-top' %>
  <div class="row card_sign m-center">
    <h2>Je suis Défieur</h2>

    <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="form-inputs">
        <%= f.input :email,
                    required: false,
                    autofocus: true,
                    input_html: { autocomplete: "email", class: 'inputForm' } %>
        <%= f.input :password,
                    required: false,
                    input_html: { autocomplete: "current-password", class: 'inputForm' } %>
        <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
      </div>

      <div class="form-actions">
        <%= f.button :submit, "SE CONNECTER", class: "btn btn-treehouse mb-3" %>
      </div>
    <% end %>
      <div class="link">
        <%= render "devise/shared/links" %>
      </div>
  </div>
  <%= render 'components/navbar-bottom-home' %>
</div>

