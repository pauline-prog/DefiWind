<%= render 'components/navbar-top-back' %>

<div class="bg-log">
  <div class="row card_log m-center1">
    <h2>Je suis Défieur</h2>

    <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="form-inputs">
        <%= f.input :email,
                    label: "Email",
                    placeholder: "oscar.berthemet@gmail.com",
                    required: false,
                    autofocus: true,
                    input_html: { autocomplete: "email", class: 'inputForm' } %>
        <%= f.input :password,
                    label: "Mot de passe",
                    placeholder: raw("&bull;&bull;&bull;&bull;&bull;&bull;"),
                    required: false,
                    input_html: { autocomplete: "******", class: 'inputForm' } %>
        <%= f.input :remember_me, label: "Se souvenir de moi", as: :boolean if devise_mapping.rememberable? %>
      </div>

      <div class="form-actions">
        <%= f.button :submit, "SE CONNECTER", class: "btn btn-treehouse mb-3" %>
      </div>
    <% end %>
      <div class="link">
        <%= render "devise/shared/links" %>
      </div>
  </div>
</div>

<%= render 'components/navbar-bottom-profile' %>
